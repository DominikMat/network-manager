version: '3.8'
services:
  envoy:
    image: envoyproxy/envoy:v1.22.0
    ports:
      - "8081:8081"
    volumes:
      - ./envoy.yml:/etc/envoy/envoy.yaml
    extra_hosts:
      - "host.docker.internal:host-gateway"